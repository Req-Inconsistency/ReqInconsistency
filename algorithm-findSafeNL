Input: 
reCCG = (reCC, reCA): cycle extended causality clock graph
ST={<si,CGi,CCGi>|1≤i≤n}
Output: 
CR: a set of safeNL of circular dependency 

CR = null;
for each st of ST
	statePairSet = getStatePair(CCG);
	if(∀<state.s,state.f> ∈ StatePairSet ∧ <state.s,state.f> ∈ reCC)
		keyCCG = getKeySet(CCG, statePairSet);
		if(keyCG ∈ reCCG)
			CR = CR ∪ s;
return CR;





